{{- $imageTag := default .Values.global.octarine.version .Values.enforcer.image.tag -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "guardrails.enforcer.fullname" . }}
  labels:
{{ include "guardrails.enforcer.labels" . | indent 4 }}
spec:
  replicas: {{ .Values.enforcer.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "guardrails.enforcer.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      annotations:
        prometheus.io/scrape: {{ .Values.enforcer.prometheus.scrape | quote }}
        prometheus.io/port: {{ .Values.enforcer.prometheus.port | quote }}
      labels:
        app.kubernetes.io/name: {{ include "guardrails.enforcer.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      serviceAccountName: {{ template "guardrails.serviceAccountName" . }}
      priorityClassName: {{ include "octarine.priorityClass.name" . }}
      containers:
        - name: {{ include "guardrails.enforcer.name" . }}
          command:
            - "/enforcer"
          env:
            {{ include "octarine.common.env" . | nindent 12 }}
            - name: GUARDRAILS_ENFORCER_PROBES_PORT
              valueFrom:
                configMapKeyRef:
                  name: {{ include "guardrails.enforcer.configmap.env.fullname . }}
                  key: probes.port
          envFrom:
            - configMapRef:
                name: {{ include "guardrails.enforcer.configmap.env.fullname" . }}
            {{ include "octarine.common.envFrom" . | nindent 12 }}
          securityContext:
            {{- toYaml .Values.enforcer.securityContext | nindent 12 }}
          image: "{{ .Values.enforcer.image.repository }}:{{ $imageTag }}"
          imagePullPolicy: {{ .Values.enforcer.image.pullPolicy }}
          {{- include "octarine.probes" . | nindent 10 }}
          ports:
            - name: https
              containerPort: 443
          resources:
            {{- toYaml .Values.enforcer.resources | nindent 12 }}
          volumeMounts:
            - name: cert
              mountPath: /etc/octarine-certificates
      volumes:
        - name: cert
          secret:
            secretName: {{ include "guardrails.enforcer.tls.secret.name" . }}
            defaultMode: 420
            optional: true
